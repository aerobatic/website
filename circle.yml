
machine:
  node:
    version: 8.2.0

dependencies:
  pre:
    - wget https://github.com/gohugoio/hugo/releases/download/v0.32.4/hugo_0.32.4_Linux-64bit.deb
    - sudo dpkg -i hugo*.deb
  override:
    - yarn install
    - yarn add aerobatic-cli
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - echo "No tests"

deployment:
  master:
    branch: master
    commands:
      - NODE_ENV=production yarn run build:aerobatic
      - aero deploy
